{
  "name": "p2p-gomoku-hub-onebox",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "postinstall": "cd app && npm i && cd ../server && npm i",
    "build": "cd app && npm run build",
    "start": "npm run build && node server/index.js",
    "dev": "node server/index.js"
  }
}