{
  "name": "p2p-gomoku-hub-split-turn",
  "private": true,
  "version": "0.1.1",
  "scripts": {
    "postinstall": "cd app && npm i && cd ../server && npm i",
    "build": "cd app && npm run build",
    "start": "npm run build && node server/index.js"
  }
}